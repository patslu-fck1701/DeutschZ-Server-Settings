# Templates

Sichere Vorlagen für lokale oder produktive Konfigurationen.

Dateien mit `.example` enthalten ausschließlich Platzhalter. Kopien mit echten Werten müssen lokal bleiben und werden durch `.gitignore` ausgeschlossen.
